# Local data directory

This file documents the location; it is not a sample dataset.

All other contents of this directory are ignored by Git. Use it only for local
databases, user-provided inputs, annotations, and generated analysis data. Never
remove the ignore protection or force-add real user data.

Public examples, when a future milestone needs them, must live in a separately
reviewed sample directory and must contain only synthetic, project-authored data.
